/**
 * Tests de utilidades o helpers
 */

describe('Utils', () => {
  it('should pass this basic test', () => {
    expect(true).toBe(true);
  });

  it('should handle basic math', () => {
    expect(1 + 1).toBe(2);
  });
});
